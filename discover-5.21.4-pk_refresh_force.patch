diff -up discover-master/libdiscover/backends/PackageKitBackend/PackageKitBackend.cpp.2~ discover-master/libdiscover/backends/PackageKitBackend/PackageKitBackend.cpp
--- discover-master/libdiscover/backends/PackageKitBackend/PackageKitBackend.cpp.2~	2023-07-26 03:56:23.185230762 +0200
+++ discover-master/libdiscover/backends/PackageKitBackend/PackageKitBackend.cpp	2023-07-26 05:46:07.451667127 +0200
@@ -527,7 +527,7 @@ void PackageKitBackend::checkForUpdates(
     if (!m_refresher) {
         acquireFetching(true);
         m_updater->clearDistroUpgrade();
-        m_refresher = PackageKit::Daemon::refreshCache(false);
+        m_refresher = PackageKit::Daemon::refreshCache(true);
 
         connect(m_refresher.data(), &PackageKit::Transaction::errorCode, this, &PackageKitBackend::transactionError);
         connect(m_refresher.data(), &PackageKit::Transaction::finished, this, [this]() {
diff -up discover-master/libdiscover/backends/PackageKitBackend/PackageKitNotifier.cpp.2~ discover-master/libdiscover/backends/PackageKitBackend/PackageKitNotifier.cpp
--- discover-master/libdiscover/backends/PackageKitBackend/PackageKitNotifier.cpp.2~	2023-07-25 18:00:15.000000000 +0200
+++ discover-master/libdiscover/backends/PackageKitBackend/PackageKitNotifier.cpp	2023-07-26 03:56:23.186230769 +0200
@@ -61,7 +61,7 @@ PackageKitNotifier::PackageKitNotifier(Q
             bool ok;
             const int days = value.toInt(&ok);
             if (!ok || days == 0) {
-                regularCheck->setInterval(24h); // refresh at least once every day
+                regularCheck->setInterval(12h); // refresh at least once every day
                 regularCheck->start();
                 if (!value.isEmpty())
                     qWarning() << "couldn't understand value for timer:" << value;
@@ -304,7 +304,7 @@ void PackageKitNotifier::refreshDatabase
     }
 
     if (!m_refresher) {
-        m_refresher = PackageKit::Daemon::refreshCache(false);
+        m_refresher = PackageKit::Daemon::refreshCache(true);
         connect(m_refresher.data(), &PackageKit::Transaction::finished, this, &PackageKitNotifier::recheckSystemUpdateNeeded);
     }
 
